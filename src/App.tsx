import React, {useState} from 'react';
import { Game } from './utils/game';

const App: React.FC = () => {
  const g = new Game();
  return (
    <div className="App">
    </div>
  );
}

export default App;
